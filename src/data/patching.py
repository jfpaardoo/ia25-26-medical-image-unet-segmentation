"""Patch extraction utilities for large medical images."""

from __future__ import annotations


def extract_patches(image, mask, patch_size, stride=None):
    """Split an image-mask pair into patches."""

    raise NotImplementedError("Implement patch extraction and reconstruction here.")